# 2026-04-14

## 작업 개요

지원금/혜택 영역을 공공데이터포털 기준으로 재구성하고, `benefits` 전용 데이터 파이프라인과 화면 구조를 분리했습니다.

## 주요 작업

- 공공데이터포털 `대한민국 공공서비스(혜택) 정보`를 기준으로 혜택 수집 스크립트를 추가했습니다.
- 사용 API는 `serviceList`, `serviceDetail`, `supportConditions` 3개로 고정했습니다.
- 혜택 데이터 저장 구조를 `public/data/benefits/index.json` + `public/data/benefits/items/*.json` 형태로 분리했습니다.
- 혜택 전용 로더를 추가하고, 홈 화면과 상세 페이지가 새 혜택 데이터 구조를 읽도록 변경했습니다.
- `/benefits` 목록 페이지를 새로 만들고, 헤더 `지원금/혜택` 링크도 목록 페이지로 연결했습니다.
- 블로그 생성 스크립트가 새 `events/items`, `benefits/items` 구조를 읽을 수 있도록 정리했습니다.

## 운영 정책 반영

- 혜택 데이터는 매일 오전 7시 30분(KST) 기준으로 조회하도록 워크플로우를 맞췄습니다.
- 중복 데이터는 `서비스ID` 기준으로 막도록 처리했습니다.
- 신청기한이 지난 혜택은 수집 단계에서 제외하고 저장본에서도 정리되도록 처리했습니다.
- 데이터가 동일하면 파일을 다시 쓰지 않도록 비교 로직을 추가했습니다.

## 수집 결과

- 전체 공공서비스 건수: `10919`
- 서울 소관기관 필터 통과 건수: `1113`
- 중복 제거 건수: `0`
- 만료 제거 건수: `34`
- 현재 노출 대상 건수: `1079`

## 관련 커밋

- `1a598e9` Add public benefits data pipeline

## 주요 파일

- `.github/workflows/deploy.yml`
- `scripts/fetch-public-benefits.js`
- `scripts/generate-blog-post.js`
- `src/app/benefits/page.tsx`
- `src/app/benefits/[id]/page.tsx`
- `src/components/BenefitsCatalog.tsx`
- `src/components/HomeBenefitsSection.tsx`
- `src/lib/public-benefits.ts`
- `src/lib/benefit-utils.ts`
- `public/data/benefits/index.json`

## 메모

- 혜택은 서울데이터가 아니라 공공데이터포털을 기준 소스로 유지합니다.
- 서울 필터는 현재 `소관기관명` 기준으로 적용되어 있습니다.
- 이후 필요하면 `지원대상 지역 문구`까지 함께 파싱하는 보강이 가능합니다.

---

## 추가 작업: 블로그 재정의

- 블로그를 `행사/혜택 재가공`이 아니라 `서울 전용 정보글`로 재정의했습니다.
- 노출 대상은 `region: 서울`, `sourceType: 정보글`인 글만 보이도록 정리했습니다.
- 블로그 주제 소스를 `scripts/seoul-blog-topics.js`로 분리하고, 서울 생활형 정보글 주제 30개를 큐로 구성했습니다.
- 주제는 `구별 로컬 코스`, `비 오는 날 실내`, `퇴근 후 2시간 코스`, `아이와 함께`, `저비용`, `혼자 가기 좋은 곳` 같은 생활형 탐색 기준으로 맞췄습니다.
- 하루 1개만 생성하고, 같은 날 이미 서울 정보글이 있으면 새 글을 만들지 않도록 처리했습니다.
- 글 생성 시 frontmatter에 `region`, `sourceType`, `sourceId`, `sourceUrl`, 선택적 `coverImage`, `coverAlt`를 강제하도록 정리했습니다.
- 주제별 이미지가 있으면 대표 이미지가 자동으로 붙고, 없으면 이미지 없이 글만 생성되도록 처리했습니다.
- 블로그 생성 모델 기본값을 `gemini-3-flash-preview`로 변경하고, `GEMINI_BLOG_MODEL` 환경변수로 덮어쓸 수 있도록 정리했습니다.

## 추가 작업: 브랜드/디자인 정리

- 파비콘을 서울타워 픽셀 느낌의 블루 계열 SVG로 교체했습니다.
- 헤더 브랜드 영역을 주황 계열에서 `몽환적인 블루` 기준으로 재설계했습니다.
- 홈 히어로 섹션 배경을 네이비-블루-시안 계열로 변경하고, 희미한 별빛 레이어를 추가했습니다.
- 홈 히어로 문구를 데이터 설명형에서 감성적이되 과하지 않은 생활형 문구로 수정했습니다.
- 홈 카드, 행사 목록, 혜택 목록, 블로그 목록/상세, 행사 상세, 혜택 상세 페이지의 주황/앰버 계열을 블루 축으로 통일했습니다.
- 컬러 구조는 다음 기준으로 정리했습니다.
  - 브랜드/히어로: `blue`
  - 행사: `sky/cyan`
  - 혜택: `blue/indigo`
  - 블로그: `sky/blue`

## 추가 작업: 현실적인 UI 방향 반영

- 화면이 `AI 템플릿`처럼 보이지 않도록, 강조 밀도를 줄이는 방향으로 디자인을 다시 정리했습니다.
- 카드의 shadow, radius, 색면, gradient 사용량을 줄이고 `정보 카드`처럼 읽히는 구조로 바꿨습니다.
- 행사/혜택 목록 페이지의 상단 칩 나열 중심 구조를 더 정돈된 필터 패널 느낌으로 조정했습니다.
- 상세 페이지도 랜딩페이지 스타일보다 `문서형 정보 레이아웃`에 가깝게 정리했습니다.
- 사용자 친화성을 위해 행사/혜택 페이지 상단 문구를 설명형에서 탐색 유도형 문구로 수정했습니다.

## 관련 커밋 추가

- `c0c3960` Refine Seoul blog generation and branding
- `480d6a8` Refine Seoul UI and blog generation

## 추가 메모

- `output/design` 아래에는 현실적인 홈/카탈로그 방향을 보기 위한 시안 SVG/PNG를 로컬 참고용으로 생성했습니다.
- 시안 파일은 생성 산출물이므로 Git에는 포함하지 않았습니다.

## 추가 작업: 통합 검색 1차 MVP

- 행사, 혜택, 블로그를 한 번에 검색할 수 있는 통합 검색 MVP를 추가했습니다.
- 검색 인덱스 생성 스크립트 `scripts/build-search-index.js`를 추가하고, `public/data/search/index.json`을 빌드 시 자동 생성하도록 연결했습니다.
- 검색 대상 공통 스키마를 `src/lib/search-types.ts`로 정리했습니다.
- 검색 랭킹과 필터 로직을 `src/lib/search-utils.ts`로 분리했습니다.
- 홈 히어로 영역에 자동완성 포함 통합 검색 입력 UI를 추가했습니다.
- `/search` 페이지를 새로 만들고, 유형/지역/진행 중 필터가 있는 결과 페이지를 구현했습니다.
- 빌드 스크립트는 `npm run build:search-index`를 먼저 실행한 뒤 Next.js 빌드를 수행하도록 정리했습니다.
- GitHub Actions 스케줄 실행 시에도 블로그 생성 뒤 검색 인덱스를 다시 만들도록 워크플로우를 수정했습니다.

## 검색 인덱스 현황

- 행사: `338건`
- 혜택: `1079건`
- 블로그: `1건`

## 검색 관련 주요 파일

- `scripts/build-search-index.js`
- `public/data/search/index.json`
- `src/app/search/page.tsx`
- `src/components/HomeUnifiedSearch.tsx`
- `src/components/SearchResultsView.tsx`
- `src/lib/search-types.ts`
- `src/lib/search-utils.ts`

## 추가 작업: 운영 고지 및 개인정보처리방침 초안

- 공개 운영에 필요한 최소 고지 문서를 추가했습니다.
- `개인정보처리방침` 페이지를 만들어 현재 기준에서 직접 수집하는 정보가 없다는 점, 기술 로그가 인프라 수준에서 처리될 수 있다는 점, Cloudflare Pages와 GitHub를 사용하는 운영 구조를 함께 명시했습니다.
- `서비스 안내` 페이지를 만들어 데이터 출처, 갱신 기준, AI 보조 블로그 성격, 면책 범위, 정정 요청 절차를 정리했습니다.
- 푸터에 `서비스 안내`, `개인정보처리방침`, `정정 요청` 링크를 추가하고, 현재 브랜드 톤에 맞게 정리했습니다.

## 운영 고지 관련 주요 파일

- `src/app/privacy/page.tsx`
- `src/app/notice/page.tsx`
- `src/components/Footer.tsx`

## 추가 작업: 운영 연락 채널과 광고·분석 고지

- 공개 운영 기준의 연락 창구를 분리하기 위해 `문의 및 정정 요청` 페이지를 추가했습니다.
- 운영 이메일 `jin3137@gmail.com`을 공식 문의 및 정정 요청 채널로 반영했습니다.
- `GitHub Issues`는 공개 오류 제보용 보조 채널로 낮추고, 개인정보가 섞이는 요청은 이메일로 보내도록 문구를 정리했습니다.
- 향후 Google Analytics 또는 Google AdSense를 실제로 활성화할 때 반영할 기준을 정리한 `광고·분석 고지` 페이지를 추가했습니다.
- 개인정보처리방침과 서비스 안내 문서에서 새 운영 고지 페이지들로 연결되도록 링크를 보강했습니다.

## 추가 주요 파일

- `src/app/contact/page.tsx`
- `src/app/disclosure/page.tsx`

## 추가 작업: 검색 UX 보정

- 홈 히어로의 빠른 검색 드롭다운이 배경 blur 레이어에 걸려 깨져 보이던 문제를 수정했습니다.
- 빠른 검색 결과에서 오른쪽 날짜 길이에 따라 제목 시작 위치가 흔들리던 레이아웃을 보정했습니다.
- `통합 검색` 버튼 텍스트가 줄바꿈되지 않도록 버튼 폭과 줄바꿈 동작을 정리했습니다.

## 추가 작업: 오늘의 서울 요약

- 홈 상단에 `오늘의 서울 요약` 카드 섹션을 추가했습니다.
- 집계 항목은 `오늘 진행 중 행사`, `이번 주 마감 혜택`, `무료 행사`, `실내 추천` 4개로 구성했습니다.
- 집계는 서버에서 계산해 정적 배포에도 그대로 반영되도록 정리했습니다.
- 각 요약 카드를 클릭하면 바로 해당 조건에 맞는 모아보기 화면으로 이동하도록 연결했습니다.
  - `/events?view=ongoing`
  - `/events?view=free`
  - `/events?view=indoor`
  - `/benefits?view=closing-this-week`
- 행사/혜택 목록 페이지는 `view` 쿼리 기준 프리셋 상태를 인식하고, 현재 보기 배지와 전체 복귀 링크를 함께 노출하도록 보강했습니다.

## 오늘의 서울 요약 관련 주요 파일

- `src/components/HomeSeoulSummary.tsx`
- `src/lib/home-summary.ts`
- `src/app/page.tsx`
- `src/components/EventsCatalog.tsx`
- `src/components/BenefitsCatalog.tsx`
- `src/app/events/page.tsx`
- `src/app/benefits/page.tsx`

## 추가 작업: E-E-A-T 표시 강화

- 블로그 상세 페이지 하단에 AI 활용 안내 문구를 추가했습니다.
- 현재 블로그 구조에 맞춰 `local-info.json`의 `link` 대신 실제 블로그 메타데이터인 `sourceUrl`을 원문 출처 링크 기준으로 사용하도록 정리했습니다.
- 연결 가능한 원문 링크가 있는 글은 출처 바로가기 영역을 노출하고, 없는 글은 공식 안내 재확인 문구를 대신 보여주도록 처리했습니다.
- 블로그 상세 페이지 상단 메타 정보에 `최종 업데이트` 날짜를 표시하도록 보강했습니다.
- 사이트 운영 목적, 데이터 출처, AI 활용 방식을 설명하는 `/about` 페이지를 추가했습니다.
- 헤더 내비게이션에 `소개` 메뉴를 추가했습니다.

## E-E-A-T 관련 주요 파일

- `src/app/blog/[slug]/page.tsx`
- `src/app/about/page.tsx`
- `src/components/Header.tsx`
- `src/lib/posts.ts`

## 추가 작업: 기술 SEO 및 구조화 데이터

- `sitemap.xml` 자동 생성을 위해 `src/app/sitemap.ts`를 추가했습니다.
- 메인 페이지, 블로그 목록, `src/content/posts`의 `.md` 글 경로를 기준으로 블로그 상세 URL이 자동 포함되도록 정리했습니다.
- `robots.txt` 생성을 위해 `src/app/robots.ts`를 추가하고, 전체 검색 로봇 접근 허용과 사이트맵 경로를 함께 노출하도록 구성했습니다.
- `src/app/layout.tsx`에 기본 메타데이터와 `WebSite` JSON-LD를 추가했습니다.
- 블로그 상세 페이지에는 `BlogPosting` JSON-LD와 `BreadcrumbList` JSON-LD를 추가했습니다.
- 메인 페이지에는 현재 실제로 노출되는 행사 카드 기준 `Event` JSON-LD, 혜택 카드 기준 `GovernmentService` JSON-LD를 추가했습니다.
- 구조화 데이터는 화면에 실제로 보이는 카드/정보와 일치하는 값만 사용하도록 맞췄습니다.

## 기술 SEO 관련 주요 파일

- `src/app/layout.tsx`
- `src/app/page.tsx`
- `src/app/blog/[slug]/page.tsx`
- `src/app/sitemap.ts`
- `src/app/robots.ts`
